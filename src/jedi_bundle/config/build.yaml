build options:

  platform: discover
  modules: intelclassic
  build: release
  configure: ecbuild
  path to build: &path_to_build ./
  cores to use for make: 6

source code options:

  path to source: *path_to_build
  user branch: develop
  github orgs:
    - JCSDA-internal
    - JCSDA

  bundles:
    #- fv3-jedi
    #- soca
    - iodaconv
